# Google Sign-In / Sign-Up (must match the backend's GOOGLE_CLIENT_ID)
NEXT_PUBLIC_GOOGLE_CLIENT_ID=

# Backend API base URL - baked in at build time, there's no server to read
# this at runtime once exported as static files (or deployed dynamically).
NEXT_PUBLIC_API_URL=
